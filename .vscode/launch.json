{
    "configuration": [
      {
        "program": "${workspaceRoot}",
      }
    ],
    "configurations": [
        {
            "name": "Launch File",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
        }
    ]
  }